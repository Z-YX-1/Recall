# Recall · 基线评测报告（R-33）

> 生成时间：2026-09-22 ｜ 数据：`eval/baseline_v1.json`、`eval/baseline_v2.json`、`eval/baseline_v1_hybrid_only.json`
> 黄金集：`eval/golden_set.jsonl`（30 题）｜ collection：`recall__bge-m3@v1__md`（65 篇 / 972 块）

## 1. 检索指标（tech.md §10）

| collection | 链路 | Recall@1 | Recall@3 | Recall@5 | Recall@10 | MRR |
| :--- | :--- | ---: | ---: | ---: | ---: | ---: |
| `recall__bge-m3@v1__md` | **hybrid + rerank（标题+正文）** ← 生产链路 | **0.767** | **0.933** | **0.933** | **1.000** | **0.860** |
| `recall__bge-m3@v1__md` | hybrid + rerank（只喂正文，修复前） | 0.467 | 0.733 | 0.767 | 0.833 | 0.601 |
| `recall__bge-m3@v1__md` | hybrid-only（`--no-rerank`，参考基线） | 0.567 | 0.667 | 0.733 | 0.867 | 0.650 |
| `recall__bge-m3@v2__md` | hybrid + rerank（标题+正文） | 0.767 | 0.933 | 0.933 | 1.000 | 0.860 |

v1/v2 完全一致 ⇒ **重灌忠实**（同模型、同切分器，R-34）。

**修复后 Recall@10 = 1.000**：30 题里每题的期望文档都进了前 10。
MRR 0.860 同时显著优于"干脆不要精排"的 0.650 —— 精排是净收益，前提是喂对东西。

### ✅ 关键发现与修复：精排必须喂「标题 + 正文」

首轮测量显示精排是**净负收益**（MRR 0.601 vs hybrid-only 0.650）。三步定位：

1. **否证「截断」假设**：用 bge-reranker-v2-m3 自己的分词器统计全部 972 个块——
   `p50=485 / p90=733 / p99=889 / max=1055` token，**仅 1 块（0.1%）超过 `MAX_LENGTH=1024`**。
   截断不是原因。（顺带确认：若用模型默认的 `max_length=512`，会有 **45%** 的块被截断，
   所以当初把上限设成 1024 是对的。）
2. **逐题对比找到规律**：精排相对 hybrid-only **变差 8 题**（多为 RRF 原本的第 1 名被压到第 2~3）、
   **变好 5 题**（多为第 4~7 名被提到第 1~6）。即：RRF 在头部已经很强，
   交叉编码器在头部加了噪声、在尾部产生了价值。
3. **证因**：把「标题路径 + 正文」而不是纯正文喂给精排，逐题 **13 题变好、0 题变差**：

   | 精排输入 | Recall@1 | Recall@3 | Recall@5 | MRR |
   | :--- | ---: | ---: | ---: | ---: |
   | 只喂块正文 | 0.467 | 0.700 | 0.767 | 0.591 |
   | **标题 + 正文** | **0.767** | **0.933** | **0.933** | **0.859** |

   原因：这些笔记的话题信号大部分在标题里（如「🎲 ai-Temperature 与确定性控制 —— 全景解析」），
   只喂正文会让 cross-encoder 失去最强的判别特征。

**修复**：`recall/rerank.py::build_rerank_document()` 统一负责拼接，检索链路改用它。
链路的**节点与顺序完全没变**（dense+sparse → RRF → 权限过滤 → rerank → 预算截断 → 合并），
变的只是喂给精排的字符串——属**实现修正**，不是 tech.md §4 的契约变更。

⚠️ 一个相反的候选（留给 R-42）：**embedding 侧现在仍然只编码块正文**。
既然标题对精排这么关键，对 embedding 做同样的处理很可能也有收益——
但那会改变全部向量，必须走重灌 + A/B，属 R-42 范围。

## 2. 名次分布（30 题，修复后）

| 名次 | 1 | 2 | 3 | 未命中 |
| :--- | ---: | ---: | ---: | ---: |
| 题数 | **23** | 4 | 1 | **0** |

（修复前：1 名 14 题、2 名 5 题、3 名 3 题、4 名 1、6 名 1、8 名 1、未命中 5。）

- Top-1 命中 23/30（76.7%）；Top-3 命中 28/30（93.3%）；**Top-10 30/30（100%）**。

## 3. 分类命中率（Top-10 内命中）

修复前：

| 语料分组 | 命中 / 总数 | 命中率 |
| :--- | ---: | ---: |
| AI 技术笔记（`AI/*.md` + MOC） | 18 / 19 | 0.95 |
| Recall 自身 spec（`project/Recall/spec/*`） | 3 / 3 | 1.00 |
| 项目文档（`project/bamboo-old/spec/*` 等） | 4 / 8 | **0.50** |

修复后全部 30/30，上一行那 4 题未命中已被精排修复一并解决——
说明它们**不是**语料结构问题，而是精排输入缺标题导致的判别力不足。

## 4. 质量观察

- **"元问题"会命中规范文档本身**：问「我笔记里关于 RAG 检索质量的结论？」时，Top-1 是
  vault 里的 `project/Recall/spec/roadmap.md`。该问题措辞与"规范/路线图"类文本的词汇分布
  更接近。**这不是 bug，是 query 类型导致的**；若希望优先返回技术笔记，
  可在 R-42 讨论"排除 spec 类文档"或引入 query 意图分类。
- **同文档多块占位**：`assemble` 的同文档合并只合并 `chunk_index` 连续的块，跨小节不合并，
  因此同一篇文档的多个片段可能一起进入 Top-3。若希望"多来源优先"，可在 R-42 加多样性重排。

## 5. 待补：Ragas 分数

`eval/eval_ragas.py` 已就绪（真跑 kb_answer → 校验 `citations` ↔ `references` ↔ 正文角标 →
Ragas `aevaluate`，DeepSeek 当裁判、本地 bge-m3 当 embedding），**等待 `.env` 里的
`DEEPSEEK_API_KEY`**。

已完成的验证（2026-09-22）：在无 key 情况下运行 `python eval/eval_ragas.py --limit 2`——
ragas / langchain / openai 依赖链导入全部通过，脚本在 Settings 检查处**干净退出**并打印
`缺少 DEEPSEEK_API_KEY：Ragas 需要裁判 LLM（写进 .env 后重跑）`。即：链路已验到 key 边界。

跑完后本节补上：

```powershell
python eval/eval_ragas.py --limit 10 --output eval/ragas_baseline.json
```

## 6. 给 R-42 的调优候选（数据驱动，按预期收益排序）

| 候选 | 依据 | 预期 |
| :--- | :--- | :--- |
| **embedding 侧也前置 `heading_path`** | 第 1 节：标题对精排把 MRR 从 0.591 拉到 0.859 | 可能进一步提升 Recall@1；需重灌 + A/B |
| 切分粒度 A/B（当前 MAX=800 / overlap≈100） | tech.md §2 提示粒度是 recall 最大单一参数 | 提升召回上限 |
| 同文档多样性约束（MMR / 每文档限流） | 第 4 节：同文档多块占位 | 让 Top-K 覆盖更多来源 |
| 是否排除 spec 类文档 / query 意图分类 | 第 4 节元问题观察 | 改善"元问题"类 query |

**所有调优都必须回到本报告同一套黄金集 + 指标上对比**（tech.md §10 触发点）。
